{
  "name": "spectrascope",
  "version": "1.0.0",
  "description": "Safe-by-default external attack-surface analysis platform",
  "private": true,
  "repository": "https://github.com/lukianchik/SpectraScope",
  "scripts": {
    "dev": "bun run --cwd frontend dev",
    "build": "bun run --cwd frontend build"
  }
}
